
<%= simple_form_for(@freegraffer) do |f| %>
  <%= f.input :description %>
  <%= f.input :style %>
  <%= f.input :price %>
  <%= f.input :disponibility %>
  <%= f.input :photo, as: :file, input_html: { multiple: true }%>
  <%= f.submit "add a freegraffer ", class: "btn btn-primary" %>
<% end %>

